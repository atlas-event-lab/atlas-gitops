apiVersion: v1
kind: Service
metadata:
  name: {{ include "atlas-service.fullname" . }}
  labels:
    {{- include "atlas-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "atlas-service.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
